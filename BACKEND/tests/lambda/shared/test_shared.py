import pytest

from shared import handler as shared_handler  # adjust when shared helpers are defined


@pytest.mark.skip(reason="TODO: implement shared Lambda/helpers")
def test_shared_placeholder():
    """
    Placeholder test for shared Lambda utilities.

    When shared helpers are implemented (e.g. normalizers, DTO mappers),
    add unit tests here that call them directly.
    """
    assert True
